import { getArticulos } from '@/data/blog'; import { notFound } from 'next/navigation'; export async function generateStaticParams() { return getArticulos().map((post: any) => ({ slug: post.slug })); } export default function BlogPost({ params }: { params: { slug: string } }) { const post = getArticulos().find((p: any) => p.slug === params.slug); if (!post) return notFound(); return (<main style={{maxWidth:'800px',margin:'0 auto',padding:'2rem'}}><h1>{post.title}</h1><div dangerouslySetInnerHTML={{ __html: post.content }} /><a href='https://wa.me/5491151797649' style={{background:'#16A34A',color:'white',padding:'1rem 2rem',borderRadius:'8px',textDecoration:'none',display:'inline-block',marginTop:'2rem'}}>?? Consultar por WhatsApp</a></main>); }
