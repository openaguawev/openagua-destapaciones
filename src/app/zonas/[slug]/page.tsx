import { getZonas } from '@/data/zonas'; import { notFound } from 'next/navigation'; export async function generateStaticParams() { return getZonas().map((zona: any) => ({ slug: zona.slug })); } export default function ZonaPage({ params }: { params: { slug: string } }) { const zona = getZonas().find((z: any) => z.slug === params.slug); if (!zona) return notFound(); return (<main style={{maxWidth:'800px',margin:'0 auto',padding:'2rem'}}><h1>{zona.name}</h1><p>{zona.description}</p><a href='https://wa.me/5491151797649' style={{background:'#16A34A',color:'white',padding:'1rem 2rem',borderRadius:'8px',textDecoration:'none',display:'inline-block',marginTop:'2rem'}}>?? Consultar por WhatsApp</a></main>); }
